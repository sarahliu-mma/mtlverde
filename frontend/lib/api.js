// Base URL for the FastAPI backend on Railway. Centralized so the events feed
// and the saved-count endpoint share one place to change.
export const API_BASE = "https://mtlverde-production.up.railway.app";
